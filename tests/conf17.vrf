# ------------------------
# complex config component
# ------------------------
mulkey = re(VAL, '^.+$')
@inherit (conf16e00.vrf)

[ sec\w+ ]
    [[ sec\w+ ]]
        keyup = re(VAL, '^.+$')  # Remove mandatory key property by overwriting
