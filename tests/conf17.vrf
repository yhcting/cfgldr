# ------------------------
# complex config component
# ------------------------
mulkey = re('^.+$')
@inherit (conf16e00.vrf)

[ sec\w+ ]
    [[ sec\w+ ]]
        keyup = re('^.+$')  # Remove mandatory key property by overwriting
