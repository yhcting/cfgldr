# ------------------------
# complex config component
# ------------------------
mulkey = .+
@inherit (conf16e00.vrf)

[ sec\w+ ]
    [[ sec\w+ ]]
        keyup = .+  # Remove mandatory key property by overwriting
