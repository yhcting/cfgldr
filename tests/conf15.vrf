# ------------------------
# complex config component
# ------------------------
keyupper.+ =  .+   # accept all
key.+ = .+ # accept all

[ subsec[a-z]+ ]
@inherit (conf14.vrf)
keyupper2 = [A-Z]+
keyupper0 = [a-z]+ # accept modification
keyupper1 = [A-Z]+
