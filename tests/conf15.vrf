# ------------------------
# complex config component
# ------------------------
keynum1 = 'int(VAL) > 0'
keyupper.+ =  re('^.+$')   # accept all
key.+ = re('^.+$') # accept all

[ subsec[a-z]+ ]
@inherit (conf14.vrf)
keyupper2 = re('^[A-Z]+$')
keyupper0 = re('^[a-z]+$') # accept modification
keyupper1 = re('^[A-Z]+$')
