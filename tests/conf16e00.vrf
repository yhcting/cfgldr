# ------------------------
# complex config component
# ------------------------

keyempty = re('^$') # accept empty
.+ = re('^.+$') # accept all

[ sec\w+ ]
    .+ = re('^.+$') # accept all(non empty)
    [[ sec\w+ ]]
    ^keyup = re('^.+$')  # Error: missing mandatory key
    .+ = re('^.+$')

        [[[ sec\w+ ]]]
        ^key\w+ = re('^.+$')
        .+ = re('^.+$')
